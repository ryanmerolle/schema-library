# yaml-language-server: $schema=https://schema.infrahub.app/infrahub/schema/latest.json
---
version: '1.0'
generics:
  - name: Generic
    namespace: Thirdparty #TODO: find something better here => Vendor, contractor ...
    label: Third Party
    description: An third Party represent a legal entity, a company.
    default_filter: name__value
    icon: mdi:domain
    include_in_menu: true
    attributes:
      - name: name
        kind: Text
        unique: true
        order_weight: 1000
      - name: description
        kind: Text
        optional: true
        order_weight: 1200
    relationships:
      - name: tags
        peer: BuiltinTag
        cardinality: many
        kind: Attribute
        optional: true
        order_weight: 3000

nodes:
  - name: Manufacturer
    namespace: Thirdparty
    description: Device Manufacturer
    default_filter: name__value
    order_by: [name__value]
    display_labels:
      - name__value
    icon: mdi:domain
    inherit_from:
      - ThirdpartyGeneric
    include_in_menu: true
    menu_placement: ThirdpartyGeneric
    relationships:
      - name: device_type
        peer: DcimDeviceType
        cardinality: many
        optional: true
      - name: platform
        peer: DcimPlatform
        cardinality: many
        optional: true
  - name: Provider
    namespace: Thirdparty
    description: Circuit or Location Provider
    default_filter: name__value
    order_by: [name__value]
    display_labels:
      - name__value
    icon: mdi:domain
    inherit_from:
      - ThirdpartyGeneric
    include_in_menu: true
    menu_placement: ThirdpartyGeneric
    relationships:
      - name: circuit
        peer: CircuitCircuit
        cardinality: many
        optional: true
